import React from 'react';
import ReactDOM from 'react-dom/client';
import TravelRouteMap from '../flight-log.jsx';

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <TravelRouteMap />
  </React.StrictMode>
);
